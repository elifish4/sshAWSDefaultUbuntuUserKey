# sshAWSDefaultUbuntuUserKey

Connect AWS instances with default keys for diffrent env more easiest way. 


1) Copy the file to /usr/bin/s (or other name that you want instead the 's' )

2) Edit the file keys location


Usage:

``` s INSTANCE_IP```
``` s INSTANCE_NAME```
